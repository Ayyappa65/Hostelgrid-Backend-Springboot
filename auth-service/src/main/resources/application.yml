spring:
  application:
    name: auth-service
  profiles:
    active:
    - dev
  datasource:
    url: jdbc:mysql://localhost:3306/hostelgrid_auth_db
    username: root
    password: Y21CSE031
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # options: none, update, create, create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    
server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# Management Endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  # Actuator 
  endpoint:
    health:
      show-details: always