# Production Profile Configuration
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://${IP:localhost}:${DB_PORT:3306}/${STUDENT_SERVICE_DB_NAME:hostelgrid_student_db}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: ${STUDENT_SERVICE_PORT:8085}

logging:
  level:
    root: WARN
    com.hostelgrid: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info